repos:
  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: uv run --frozen pytest -q -p no:cacheprovider
        language: system
        pass_filenames: false
        stages: [pre-commit]
